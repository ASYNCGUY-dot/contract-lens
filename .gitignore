# 비밀정보 — 절대 커밋 금지
.env
.env.*
!.env.example
*.bak
*.key

# 파이썬
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# 노드
node_modules/
dist/

# 데이터·산출물 (용량)
data/raw/
*.db
*.sqlite3
*.log

# OS
.DS_Store
Thumbs.db
_items.json

# 오피스 프로그램 잠금 파일
.~lock.*#
~$*
_items_v2.json

# ONNX 변환 결과 (441MB). build_onnx.py 로 언제든 다시 만든다.
models/

# 후기 게시판 — 남의 글이다. 절대 커밋하지 않는다.
data/feedback.db
data/_fb_test.db

# 터널 실행 파일 (53MB). 공식 저장소에서 다시 받으면 된다.
tools/cloudflared.exe

# 판례 본문 (7.5MB). 저작권법상 판례 자체는 보호 대상이 아니나
# 법제처 API 이용약관의 재배포 조항을 확인하지 않았으므로 공개 리포에
# 넣지 않는다. collect_cases.py 로 다시 받는다.
data/cases/bodies.jsonl
